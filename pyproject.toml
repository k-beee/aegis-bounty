[project]
name = "aegis-bounty"
version = "0.1.0"
description = "Decentralized Bug Bounty & Exploit Adjudication Protocol on GenLayer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
